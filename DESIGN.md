# ROAST NOTE Design Guide

このドキュメントは、今後追加するセクションやUIのデザインを統一するための基準である。現行の完成済み実装（`dist/index.html`、`dist/styles.css`）のうち、ヘッダー、ファーストビュー、ファーストビュー直下の「選ばれる4つの理由」、「3分で見つかる、あなた好みのコーヒー」を参照している。

記載した数値は、特記がない限り現行CSSの実値である。新規実装では見た目だけを似せるのではなく、既存の色・書体・幅・余白・CTA・レスポンシブ挙動を再利用すること。

## 1. ブランド・デザインコンセプト

ROAST NOTEは、スペシャルティコーヒーを日々の生活へ自然に取り入れる、上品で落ち着いたブランドとして表現する。

- 基調は「上品」「ナチュラル」「落ち着き」「温かみ」「丁寧な暮らし」「コーヒーブランドらしさ」。
- 生成りの背景、深いグリーン、ブラウン、テラコッタを中心に、焙煎した豆、紙、木、葉、自然光を連想させる世界観を維持する。
- 明朝体、余白、細い線、控えめな手書き文字により、静かで編集的な佇まいをつくる。
- 英字ラベルや手書きコピーは、情報の主役ではなく、ブランドの空気感を補う要素として使う。
- 派手なグラデーション、鮮やかすぎる色、大量のカード、強い発光、過剰な立体感など、一般的なWebサービス風・AI生成サイト風の表現は避ける。
- 装飾よりも、写真、言葉、余白、読みやすい情報設計を優先する。

## 2. カラールール

### 基本カラートークン

| 用途 | 現行値 | 使用方針 |
| --- | --- | --- |
| ベース背景 / Ivory | `#f5f0e8` | `body` の背景。温かい生成りをサイト全体の基調にする。 |
| 明るい背景 / Warm White | `#faf7f1` | ヘッダーと「選ばれる4つの理由」の背景。白を使う場合も、原則としてこの温度感へ寄せる。 |
| 診断セクション背景 | `linear-gradient(135deg, #fcf9f4 0%, #f6f0e7 100%)` | ごく弱いアイボリーの濃淡。派手なグラデーション用途ではない。 |
| 基本文字 / Brown | `#382a24` | 本文と標準見出し。純黒は原則使用しない。 |
| メイン / Deep Green | `#173d2b` | ブランド、ヘッダーCTA、診断UI、主要な状態表示。 |
| アクセント・主要CTA / Terracotta | `#bb5333` | 診断CTA、価格ラベル、細いアクセント。限定的に使う。 |
| 境界線 | `rgba(56, 42, 36, 0.18)` | 区切り線。強い罫線にしない。 |

### 現行の補助色

- ヘッダータグライン: `#26352d`
- ナビゲーション文字: `#2e2824`
- 理由番号: `#664a3d`
- 手書きラベル: `#87786f`
- 診断英字ラベル: `#945037`
- ステップ番号: `#8f4934`
- ステップアイコン: `#72432f`
- ステップアイコン背景: `rgba(230, 220, 205, 0.72)`
- CTAホバー: `#a94328`（テラコッタ）、`#0e3020`（グリーン）
- 白文字: `#ffffff`

補助色は、同じ役割の既存色が使えない場合にだけ使用する。新しい色を安易に追加せず、まず上記トークンと透明度の調整で解決する。色を追加する場合は、既存パレットとの役割の違いを説明できること。

## 3. タイポグラフィ

### フォントファミリー

| 役割 | 現行指定 | 主な用途 |
| --- | --- | --- |
| 日本語サンセリフ | `"Noto Sans JP", "Hiragino Kaku Gothic ProN", sans-serif` | 本文、ナビゲーション、CTA |
| 日本語セリフ | `"Noto Serif JP", "Yu Mincho", serif` | 大見出し、小見出し、価格補足、CTA下補足 |
| 英字セリフ | `"Cormorant Garamond", Georgia, serif` | 英字ラベル、連番、STEP表記 |
| 手書き日本語 | `"Yuji Syuku", "Klee One", cursive` | 手書き風コピー |
| 手書き英字 | `"Caveat"` を優先し、上記英字／手書き書体へフォールバック | `Why Choose Us?`、装飾的英字コピー |

Google Fontsで読み込んでいるウェイトは、`Noto Sans JP: 400/500/600`、`Noto Serif JP: 400/500`、`Cormorant Garamond: 400/500`、`Caveat: 400`、`Yuji Syuku: 400`。新規実装でも基本的にこの範囲を使う。

### 文字スタイル

- 大見出し
  - ファーストビュー: セリフ、`clamp(3.65rem, 4.9vw, 4.6rem)`、`font-weight: 400`、`line-height: 1.29`、`letter-spacing: 0.045em`。
  - 900px以下: `clamp(2.7rem, 9vw, 4rem)`。
  - 560px以下: `clamp(2.05rem, 9.4vw, 2.7rem)`、`line-height: 1.38`、`letter-spacing: 0.025em`。
  - 診断セクション: セリフ、`clamp(2.7rem, 3.65vw, 4rem)`、`font-weight: 400`、`line-height: 1.42`、`letter-spacing: 0.045em`。560px以下では `clamp(2rem, 10vw, 2.8rem)`、`line-height: 1.45`。
- セクション小見出し
  - 「選ばれる4つの理由」: セリフ、`1.55rem`、`font-weight: 500`、`letter-spacing: 0.07em`。
  - 理由タイトル: セリフ、`clamp(0.92rem, 1.05vw, 1.08rem)`、`font-weight: 600`、`line-height: 1.45`、`letter-spacing: 0.04em`。
  - 診断ステップタイトル: セリフ、`1.28rem`、`font-weight: 600`、`letter-spacing: 0.06em`。560px以下は `1.08rem`。
- 英字ラベル
  - ファーストビュー: `0.98rem`、`font-weight: 500`、`letter-spacing: 0.24em`。
  - 診断セクション: `0.92rem`、`font-weight: 500`、`letter-spacing: 0.28em`。
  - STEP: `0.91rem`、`letter-spacing: 0.22em`。数字は `1.42rem`。
  - 大文字と広い字間を基本とし、本文の代わりには使わない。
- 本文
  - 標準はサンセリフ、`font-weight: 400`。
  - ファーストビュー: `clamp(0.93rem, 1.15vw, 1.08rem)`、`line-height: 1.9`、`letter-spacing: 0.07em`。
  - 診断リード: `1rem`、`line-height: 2`、`letter-spacing: 0.06em`。
  - 理由本文: `clamp(0.75rem, 0.86vw, 0.88rem)`、`line-height: 1.75`、`letter-spacing: 0.04em`。
  - 診断ステップ本文: `0.88rem`、`line-height: 1.85`、`letter-spacing: 0.04em`。
- CTA
  - サンセリフ、`font-weight: 500`。
  - ヒーローCTA: `clamp(1rem, 1.35vw, 1.18rem)`。
  - 診断セクションCTA: `1.08rem`、`line-height: 1`、`letter-spacing: 0.08em`。

文字サイズは画面幅に追従する必要がある箇所では、現行と同様に `clamp()` を使用する。日本語本文へ過剰な字間を付けず、見出しの品位と本文の読みやすさを分けて設計する。

## 4. コンテンツ幅

中央配置は `width` と `max-width` 相当の `min()`、左右 `margin: 0 auto` を基本とする。現行値は次のとおり。

| 対象 | デスクトップ | 中間幅 | 狭い画面 |
| --- | --- | --- | --- |
| ヘッダー | `width: min(100% - 48px, 1800px)`（片側24px） | 1180px以下: `min(100% - 36px, 1120px)` | 900px以下: `calc(100% - 32px)`（片側16px） |
| ファーストビュー | `width: min(100% - 48px, 1800px)` | 1180px以下: `calc(100% - 36px)`、900px以下: `calc(100% - 44px)` | 560px以下: `calc(100% - 34px)`（片側17px） |
| 選ばれる理由 | `width: min(100% - 112px, 1740px)`（片側56px） | 1180px以下: `calc(100% - 40px)` | 900px以下: `min(100% - 44px, 760px)` |
| 診断セクション | `width: min(calc(100% - clamp(64px, 7.8vw, 112px)), 1380px)` | 1100px以下: `calc(100% - 64px)`、960px以下: `min(100% - 56px, 720px)` | 560px以下: `calc(100% - 34px)` |

- ワイド画面ではコンテンツを無制限に広げず、上記の最大幅で中央に留める。
- 新しい通常セクションは、情報量の多い2カラムなら診断セクションの最大 `1380px`、横長の帯セクションなら理由セクションの最大 `1740px` を第一候補とする。
- 新しい幅を作る前に、既存4系統のどれを再利用できるか確認する。
- `100vw` 前提で子要素を固定幅にせず、左右ガターを必ず確保する。

## 5. セクション余白

### 現行の基準

- ヘッダー: 高さ `78px`。900px以下は `68px`。
- ファーストビュー
  - セクション高: `min-height: clamp(690px, calc(100vh - 78px), 800px)`。
  - コピー上余白: `clamp(78px, 9vh, 106px)`。900px以下は `64px`、560px以下は `48px`。
  - 英字ラベル→見出し: `22px`。560px以下は `18px`。
  - 見出し→価格: `22px`。560px以下は `16px`。
  - 見出し群→本文: `10px`。560px以下は `20px`。
  - 本文→CTA: `25px`。560px以下は `22px`。
- 選ばれる4つの理由
  - デスクトップは `min-height: 242px` の横長帯。
  - 理由リスト上下: `24px`。
  - 各理由: 上下 `9px`、左右 `clamp(18px, 2vw, 32px)`。
  - 理由アイコン行→タイトル: `7px`、タイトル→本文: `8px`。
  - 900px以下: セクション内上 `50px`、下 `36px`。イントロ下 `36px`。各カード `26px`、`min-height: 200px`。
  - 560px以下: 各項目の上下 `28px`、左 `58px`、右 `4px`。
- 3分で見つかる、あなた好みのコーヒー
  - セクション上下: 上 `clamp(110px, 8.5vw, 148px)`、下 `clamp(80px, 6.5vw, 110px)`。
  - 960px以下: 上 `88px`。560px以下: 上 `68px`、下 `58px`。
  - 見出しブロック→ステップ: `54px`。1100px以下は `46px`。960px以下では構造を縦並びにし、全体gap `48px`。
  - 英字ラベル→見出し: 下 `24px`に加え、ラベル下線まで `18px`。560px以下のラベル下は `18px`。
  - 見出し→リード: `26px`。560px以下は `18px`。
  - ステップ間: `36px`。
  - 2カラム間: `clamp(44px, 4.2vw, 64px)`。1100px以下は `40px`。
  - CTA上: 通常 `10px`、960px以下 `18px`。CTA→補足は `15px`。

新規セクションでは、上下余白をモバイルで単純に半減させない。見出し、本文、画像の密度を見ながら、既存の `68px`、`88px`、`110px` 前後の段階を使う。余白の追加は `margin` の積み重ねより、親の `gap` を優先する。

## 6. レイアウトルール

- ヘッダー: デスクトップは `grid-template-columns: 250px minmax(0, 1fr) 250px`。1180px以下は `210px 1fr 180px`。900px以下はFlexboxへ切り替え、ロゴとメニューボタンを両端配置する。
- ファーストビュー: 写真を全面背景として配置し、コピーは `width: min(52vw, 760px)`。900px以下では `min(100%, 570px)`。背景写真とシェードのみ absolute とし、本文は通常フロー内に置く。
- 選ばれる理由: デスクトップは `294px + 1fr` の2領域。理由4項目は `repeat(4, minmax(0, 1fr))`。1180px以下は導入部 `255px`、900px以下は導入部＋2列、560px以下は1列。
- 診断セクション: デスクトップは `repeat(2, minmax(0, 1fr))` の均等2カラム。960px以下で1カラム化し、見出し、ステップ、端末ビジュアルの順に並べる。
- 診断ステップ: `88px + minmax(0, 1fr)`、gap `28px`。1100px以下は `82px + 1fr`、gap `22px`。560px以下は `72px + 1fr`、gap `17px`。
- 子要素の縮小を妨げないよう、Grid列に `minmax(0, 1fr)`、必要な要素に `min-width: 0` を使う。
- 主要レイアウトはGrid/Flexboxと `gap` で構成する。absoluteは背景、端末モック、豆や葉、手書きコピーなどの装飾に限定する。

PCでは内容に応じて2カラムを基本とし、狭い画面では読み順を保った1カラムへ切り替える。単に縮小して2カラムを維持しない。

## 7. CTAルール

### 主要CTA（診断開始）

- 背景: `#bb5333`。
- 文字: `#ffffff`、サンセリフ、`font-weight: 500`。
- 形状: `border-radius: 999px` のピル型。
- ヒーローCTA: `min-height: 70px`、左右padding `37px`、要素間gap `22px`、最大幅を持つ親 `530px`。560px以下は高さ `64px`、左右 `20px`、gap `12px`。
- 診断セクションCTA: 親幅 `min(100% - 44px, 580px)`、ボタン高さ `72px`、左右padding `36px`。560px以下は高さ `64px`、左右 `20px`。
- 矢印: 右向きの `→` を文字として置き、本文との間隔を確保する。新しい矢印アイコンを混在させない。
- 影: ヒーローは `0 12px 28px rgba(47, 22, 13, 0.22)`、診断セクションは `0 13px 28px rgba(83, 41, 26, 0.18)`。影は暖色かつ低コントラストにする。
- hover/focus-visible: 背景 `#a94328`、上へ `1〜2px`。transitionは `180ms ease`。
- ボタン周囲には本文から `22〜25px`、補足文までは `15px`を目安に余白を取る。

### 補助CTA（ヘッダー）

- 背景: `#173d2b`、hover `#0e3020`。
- 幅 `218px`、`min-height: 52px`、左右padding `25px`、gap `22px`、ピル型。
- 1180px以下ではカラム幅に合わせて `width: 100%`。900px以下ではデスクトップCTAを隠し、モバイルメニュー内で高さ `52px`の同系CTAを使う。

同じ「診断開始」用途に、新しい形、色、矢印、アニメーションを追加しない。既存のテラコッタCTAを再利用する。ヘッダーの常設導線だけは既存のグリーンCTAを使う。

## 8. アイコンルール

- 「選ばれる4つの理由」は、現行の同一テイストの画像アイコンを `68px × 68px`、`object-fit: contain` で使用している。4項目内で描画密度、余白、色味が異なる素材を混在させない。
- 診断ステップは、細い線画SVGを使用する。
  - 線: `stroke: currentColor`、`stroke-width: 1.7`、端と接合はround。
  - 色: `#72432f`。
  - SVG: `44px × 44px`。
  - 背景円: `88px × 88px`、`rgba(230, 220, 205, 0.72)`、`border-radius: 50%`。
  - 配置: `display: grid; place-items: center` で完全に中央へ置く。
  - 1100px以下: 円 `82px`、SVG `41px`。560px以下: 円 `70px`、SVG `36px`。
- 新しい線画アイコンは、塗りつぶし、太い線、カラフルなUIアイコン、3Dアイコンと混在させない。
- 背景円付き線画は手順説明、画像アイコンはブランド特長という現行の役割分担を守る。同一の反復UI内では必ず一つの方式に統一する。

## 9. 写真・画像ルール

- 自然光、暖かい色温度、木や生成りの素材、コーヒーのある生活、丁寧な暮らしが伝わる写真を選ぶ。
- 人物や生活シーンは、作り込みすぎた広告感より、静かな日常と余白を感じるものを優先する。
- ファーストビュー写真は領域全体を覆う `object-fit: cover` を基本とし、文字の可読性を損なわない焦点位置を画面幅ごとに調整する。現行はデスクトップ `50% 44%`、900px以下 `54% center`、560px以下 `52% center`。
- 現行写真には `saturate(0.9) contrast(1.02)` を適用し、彩度をわずかに抑えている。新規写真も色温度と彩度を既存写真へ揃える。
- 写真上の文字には、ブラウン〜黒の低彩度シェードを重ねて可読性を確保する。シェードを強くしすぎて写真の自然さを失わない。
- 端末や商品画像の影は、黒く硬い影ではなく、暖色を含む柔らかなdrop-shadowを使う。
- 派手なネオン、寒色中心の無機質な写真、過剰に整いすぎたCG、質感や光源が不自然なAI感の強い画像は避ける。

## 10. 装飾ルール

- コーヒー豆、葉、淡い円形背景、1pxのライン、手書き風コピー、英字コピーをブランド装飾として使用できる。
- 色はブラウン、グリーン、テラコッタ、ベージュの既存パレット内に留める。
- 円形背景は低コントラストで使用する。診断ビジュアルの現行値は `rgba(230, 220, 204, 0.66)` と `rgba(239, 231, 218, 0.78)`。
- ラインは原則 `1px`。区切り線は `rgba(56, 42, 36, 0.18)`、アクセント線はテラコッタ系の半透明色を使う。
- 手書きコピーは軽い回転（現行は概ね `-7deg`〜`-8deg`）を許容するが、多用しない。ファーストビューの手書きコピーは900px以下で非表示にしている。
- 豆・葉はabsolute配置を許容するが、`pointer-events: none` とし、本文より低い視覚優先度にする。小画面では一部を非表示にして密度を下げる。
- 英字コピーは大文字＋広い字間、またはCaveatの手書き表現とし、日本語見出しと競合させない。
- 装飾が本文、CTA、重要な画像の顔や商品、フォーカス表示に重ならないこと。装飾のために不自然な空白を作らないこと。

## 11. レスポンシブ設計

### 現行ブレークポイント

- `1180px`: ヘッダー、ヒーロー、理由セクションの横幅とカラムを圧縮。
- `1100px`: 診断セクションのgap、ステップアイコン、端末画像を縮小。
- `960px`: 診断セクションを2カラムから1カラムへ変更。
- `900px`: ヘッダーをモバイルメニューへ変更。理由セクションを2列へ変更。ヒーローの構図とシェードを中間画面向けに変更。
- `560px`: ヒーロー、理由、診断をスマートフォン向けに再調整。理由は1列化し、装飾を間引く。

1024〜768pxは独立した重要レンジとして扱う。現行では1024px付近で「圧縮されたデスクトップヘッダー＋2カラムの診断」、960px以下で「2カラムの理由＋1カラムの診断」、900px以下で「モバイルヘッダー」が段階的に適用される。この切替順を壊さない。

実装後は最低限、`1440 / 1280 / 1024 / 900 / 768 / 600 / 390px` の各幅で確認する。境界値については、`1180 / 1100 / 960 / 900 / 560px` の直前・直後も確認する。

以下を禁止する。

- 横スクロール
- 文字と画像の重なり
- 意味や読みやすさを損なう不自然な改行
- 中間画面幅だけで起きるレイアウト崩れ
- 装飾と本文・CTAの重なり
- 固定高やabsolute配置が原因の不自然な大きな空白

`white-space: nowrap` は十分な幅が保証できる見出しや短いラベルにだけ使い、狭い画面では解除する。改行は見た目だけでなく、日本語として自然なまとまりで調整する。

## 12. 実装ルール

- 主要レイアウトはCSS GridまたはFlexboxを使う。
- 要素間隔は親の `gap` を優先し、個別marginの連鎖を避ける。
- コンテンツ幅は `max-width`、`min()`、左右ガターで制御し、中央配置する。
- 可変列には `minmax(0, 1fr)` を使い、縮小時のはみ出しを防ぐ。
- 画面幅に連動する文字、余白、サイズは `clamp()` を使う。
- レイアウト切替は既存のmedia queryを基本とし、必要な場合のみ理由を持って追加する。
- 主要レイアウトを `position: absolute` と固定pxだけで組まない。absoluteは背景画像、シェード、豆・葉、円形背景、手書きコピー、端末モックなどの装飾用途を中心にする。
- 画像には適切な `object-fit`、`object-position`、最大幅を設定する。
- hoverだけで状態を示さず、`focus-visible` も同じ品質で用意する。
- `prefers-reduced-motion: reduce` では、現行と同様にスムーズスクロールと長いtransitionを抑制する。
- CSS変数 `--ivory`、`--warm-white`、`--brown`、`--green`、`--terracotta`、`--line`、およびフォント変数を優先的に使う。

## 13. 今後のセクション追加ルール

新しいセクションを実装するときは、次の順で進める。

1. この `DESIGN.md` を確認する。
2. `dist/index.html` と `dist/styles.css` にある完成済み4セクションを確認する。
3. 既存のカラー、タイポグラフィ、コンテンツ幅、余白、CTA、アイコン表現を再利用する。
4. 新しい色、書体、ボタン形状、カード表現、装飾を安易に追加しない。
5. 追加する表現が必要な場合は、既存ルールで解決できない理由と、サイト全体で再利用できる役割を明確にする。
6. `1440 / 1280 / 1024 / 900 / 768 / 600 / 390px` と既存ブレークポイント前後で、レイアウト、改行、画像、装飾、CTAを確認する。
7. 横スクロール、重なり、不自然な空白、フォーカス欠落がないことを確認して完了とする。

このガイドと実装が食い違う場合は、まず完成済み4セクションの現行コードを事実として確認する。そのうえで、意図的に基準を変更する場合だけ `DESIGN.md` と実装を同時に更新する。

